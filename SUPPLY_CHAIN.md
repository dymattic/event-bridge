# SUPPLY_CHAIN.md

Direct deps only; every row justifies existence. Pin exact versions **≥7 days
old at pin time** (no `@latest`, no ranges). Transitives audited via lockfile.
Bumps: new row or edit + release-date arithmetic in Notes.

| Package | Version | Why | Notes |
|---|---|---|---|
| _(none yet)_ | | | Scaffold pending - add rows as deps land. |

Shipped extension bundle target: zero runtime deps beyond our code if feasible
(fetch + DOM + WebExtension APIs cover the core job).
